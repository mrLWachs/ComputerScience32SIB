
/** Required package class namespace */
package testing;

 
/**
 * Tester.java - the unit testing class that drives the various unit tests
 *
 * @author Mr. Wachs
 * @since 28-Feb-2022, 10:41:42 AM
 */
public class Tester 
{
    
    /**
     * A method (also thought of as an "action", "function", "procedure") is
     * a section (module) of code that performs an action - it may need data
     * (information) "passed" into the method (a input(s)) called a
     * parameter(s) and it may send back information (output) which it "returns"
     * a "special" method is written in classes called a "constructor" method
     * that is called "invoked" or run when the class is created (making an
     * object, "instantiated" - means make into an "instance" of that class).
     * Constructor methods have the same name as the class (includes a capital
     * letter at the start) - and they have NO return type (not even the word 
     * "void") - constructor methods are "public"
     */
    public Tester() {
        
    }
    
    
}